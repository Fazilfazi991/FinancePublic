import {describe,expect,it} from 'vitest';
import {hashLinkToken,validDraftCallback,validWebhookSecret} from './security';
import {parseQuickEntry} from '../quick-entry/parser';
import {ambiguousMessage,draftMessage} from './messages';
describe('Telegram security helpers',()=>{it('hashes tokens deterministically without storing raw values',()=>{expect(hashLinkToken('one')).toBe(hashLinkToken('one'));expect(hashLinkToken('one')).not.toContain('one')});it('validates webhook secrets safely',()=>{expect(validWebhookSecret('secret','secret')).toBe(true);expect(validWebhookSecret('wrong','secret')).toBe(false);expect(validWebhookSecret(null,'secret')).toBe(false)});it('accepts only opaque draft callbacks',()=>{expect(validDraftCallback('qe_confirm:10000000-0000-4000-8000-000000000001')).toBe(true);expect(validDraftCallback('qe_confirm:500:account')).toBe(false)})});
describe('Telegram Quick Entry adapter',()=>{const options={today:'2026-09-05',defaultAccountId:'10000000-0000-4000-8000-000000000001',debts:[{id:'20000000-0000-4000-8000-000000000002',name:'Credit Card'}]};it.each([['biryani 500','expense'],['salary 120000','income'],['paid 10000 credit card','debt_payment']])('reuses parser for %s',(text,type)=>expect(parseQuickEntry(text,options).type).toBe(type));it('keeps ambiguous input unconfirmable',()=>expect(parseQuickEntry('500',options).confidence).toBe('low'));it('formats a professional draft',()=>{const draft=parseQuickEntry('biryani 500',options);expect(draftMessage(draft,'Main Bank')).toContain('Food & Dining')});it('gives corrective examples',()=>expect(ambiguousMessage(500)).toContain('salary 50000'))});
