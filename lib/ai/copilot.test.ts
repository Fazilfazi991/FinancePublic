import{describe,expect,it}from'vitest';import{simulateExtra,compareStrategies}from'./context';import{selectToolContext}from'./tools';import{periodStart}from'./usage';import{validateQuestion,SYSTEM_PROMPT}from'./safety';import type{FinanceSnapshot}from'./types';
const snapshot:FinanceSnapshot={currency:'INR',strategy:'avalanche',freedomNumber:100000,originalDebt:120000,paidAmount:20000,percentCleared:16.7,monthlyMinimums:5000,availableBalance:10000,eligibleAccountCount:1,monthlyIncome:50000,livingExpenses:30000,debtPayments:5000,payoffPower:20000,nextDebt:'Card',debtFreeEstimate:null,debts:[{name:'Card',balance:40000,original:50000,apr:20,minimum:3000},{name:'Loan',balance:60000,original:70000,apr:10,minimum:2000}],spending:[{category:'Food',current:5000,previous:4000}],goals:[],recent:[],missing:[]};
describe('copilot deterministic tools',()=>{it('simulates extra payment using the debt engine',()=>{const r=simulateExtra(snapshot,10000);expect(r.proposedMonths!).toBeLessThan(r.currentMonths!);expect(r.monthsSaved).toBeGreaterThan(0)});it('compares both strategies deterministically',()=>{const r=compareStrategies(snapshot);expect(r.avalanche.freedomNumber).toBe(100000);expect(r.snowball.freedomNumber).toBe(100000)});it('selects server analysis for extra-payment questions',()=>expect(selectToolContext('What if I pay ₹10,000 extra?',snapshot).simulation).toBeTruthy());it('limits input and treats descriptions as data',()=>{expect(validateQuestion('x'.repeat(801))).toBeNull();expect(SYSTEM_PROMPT).toContain('untrusted quoted data')});it('uses a stable monthly UTC period',()=>expect(periodStart()).toMatch(/^\d{4}-\d{2}-01$/))});
