import{describe,expect,it}from'vitest';import{comparison,resolveAnalyticsRange}from'./date-range';import{funnelRows,retentionLabel}from'./metrics';
const now=new Date('2026-09-07T18:00:00Z');
describe('analytics date ranges',()=>{it('resolves rolling and previous ranges',()=>{expect(resolveAnalyticsRange({preset:'7d'},now)).toMatchObject({from:'2026-09-01',to:'2026-09-07',previousFrom:'2026-08-25',previousTo:'2026-08-31',days:7})});it('supports yesterday and calendar months',()=>{expect(resolveAnalyticsRange({preset:'yesterday'},now).from).toBe('2026-09-06');expect(resolveAnalyticsRange({preset:'previous_month'},now)).toMatchObject({from:'2026-08-01',to:'2026-08-31'})});it('normalizes reversed custom dates',()=>expect(resolveAnalyticsRange({from:'2026-09-07',to:'2026-09-01'},now)).toMatchObject({preset:'custom',from:'2026-09-01',to:'2026-09-07'}))});
describe('analytics calculations',()=>{it('handles growth and divide by zero',()=>{expect(comparison(118,100).label).toBe('+18.0%');expect(comparison(0,0).label).toBe('No change');expect(comparison(4,0).label).toBe('New')});it('calculates funnel conversion and drop-off',()=>{const rows=funnelRows([{key:'a',label:'A',users:100},{key:'b',label:'B',users:75},{key:'c',label:'C',users:30}]);expect(rows[1]).toMatchObject({fromPrevious:75,fromFirst:75,dropoff:25,dropoffPercent:25});expect(rows[2].dropoff).toBe(45)});it('does not fabricate future retention',()=>expect(retentionLabel(null,false)).toBe('Not yet measurable'))});
