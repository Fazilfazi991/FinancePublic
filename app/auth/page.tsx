import {AuthForm} from './auth-form';
export default async function AuthPage({searchParams}:{searchParams:Promise<{error?:string}>}){const{error}=await searchParams;return <main className="flex min-h-dvh items-center justify-center bg-background p-5"><section className="w-full max-w-sm rounded-3xl border bg-card p-6 shadow-sm"><p className="text-sm font-semibold text-primary">Finance</p><h1 className="mt-2 text-3xl font-bold">Your path to debt freedom</h1><p className="mb-7 mt-2 text-sm text-muted-foreground">Sign in securely to keep your finances private.</p>{error&&<p role="alert" className="mb-4 rounded-xl bg-destructive/10 p-3 text-sm text-destructive">That sign-in link is invalid or has expired. Request a new link below.</p>}<AuthForm/><p className="mt-6 text-xs text-muted-foreground">Your data is isolated to your account. INR is the default currency.</p></section></main>}
