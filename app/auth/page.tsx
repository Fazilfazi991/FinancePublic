import {AuthForm} from './auth-form';
export default async function AuthPage({searchParams}:{searchParams:Promise<{error?:string;next?:string}>}){const{error,next:rawNext}=await searchParams;const next=rawNext?.startsWith('/')&&!rawNext.startsWith('//')?rawNext:'/overview';return <main className="flex min-h-dvh items-center justify-center bg-background p-5"><section className="w-full max-w-sm rounded-2xl bg-card p-6 shadow-xl ring-1 ring-border"><p className="text-sm font-semibold text-primary">ZeroDebt</p><h1 className="mt-2 text-3xl font-bold">Welcome back</h1><p className="mb-7 mt-2 text-sm text-muted-foreground">Sign in to continue your debt-free journey.</p>{error&&<p role="alert" className="mb-4 rounded-xl bg-destructive/10 p-3 text-sm text-destructive">That sign-in link is invalid or has expired. Request a new email below.</p>}<AuthForm next={next}/><p className="mt-6 text-xs text-muted-foreground">Your data is private to your account. INR is the default currency.</p></section></main>}
