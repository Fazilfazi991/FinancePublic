import {NextResponse} from 'next/server'; import {apiError,requireUser} from '@/lib/api-auth';
export async function GET(){const a=await requireUser();if('response'in a)return a.response;const tables=['debts','accounts','transactions','incomes','goals','budgets'] as const;const rows=await Promise.all(tables.map(t=>a.supabase.from(t).select('*').eq('user_id',a.user.id)));if(rows.some(x=>x.error))return apiError('Unable to load your workspace',500);const[d,ac,t,i,g,b]=rows.map(x=>x.data??[]);return NextResponse.json({debts:d.map((x:any)=>({id:x.id,name:x.name,total:Number(x.original_amount),balance:Number(x.balance),rate:x.apr==null?null:Number(x.apr),minPayment:x.minimum_payment==null?null:Number(x.minimum_payment),notes:x.notes,color:x.color,isDemo:x.is_demo})),accounts:ac.map((x:any)=>({id:x.id,name:x.name,institution:x.institution,type:x.type,currency:x.currency,openingBalance:Number(x.opening_balance),color:x.color,createdAt:x.created_at,isDefault:x.is_default,notes:x.notes,tag:x.tag,icon:x.icon,isDemo:x.is_demo})),transactions:t.map((x:any)=>({id:x.id,type:x.type,amount:Number(x.amount),accountId:x.account_id,toAccountId:x.to_account_id,category:x.category,description:x.description,date:x.transaction_date,currency:x.currency,createdAt:x.created_at,incomeStreamId:x.income_stream_id,notes:x.notes,isDemo:x.is_demo})),incomes:i.map((x:any)=>({id:x.id,name:x.name,type:x.type,status:x.status,currency:x.currency,expectedMonthly:Number(x.expected_monthly),actualThisMonth:Number(x.actual_this_month),notes:x.notes,color:x.color,icon:x.icon,linkedAccountId:x.linked_account_id??'',isDemo:x.is_demo})),goals:g.map((x:any)=>({id:x.id,name:x.name,target:Number(x.target),saved:Number(x.saved),deadline:x.deadline,description:x.description,category:x.category,notes:x.notes,createdAt:x.created_at,isDemo:x.is_demo})),expenses:b.map((x:any)=>({id:x.id,name:x.name,budgeted:Number(x.budgeted),spent:Number(x.spent),category:x.category,isDemo:x.is_demo}))})}
