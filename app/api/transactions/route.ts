import {insert,list,mapTransaction} from '@/lib/finance-api'; export const GET=()=>list('transactions','transaction_date'); export const POST=async(r:Request)=>insert('transactions',mapTransaction(await r.json()));
