import Link from 'next/link';import { ShieldCheck,ArrowLeft } from 'lucide-react';import { requireAdmin } from '@/lib/admin-auth';import { AdminNav } from '@/components/admin/admin-nav';
export default async function AdminLayout({children}:{children:React.ReactNode}){const{identity}=await requireAdmin();return <div className="admin-shell -m-4 min-h-dvh bg-background lg:-m-8"><header className="border-b border-border bg-card"><div className="mx-auto flex max-w-[1500px] items-center justify-between gap-4 px-4 py-4 lg:px-8"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-primary text-primary-foreground"><ShieldCheck className="h-5 w-5"/></span><div><p className="font-semibold leading-tight">ZeroDebt Admin</p><p className="text-xs text-muted-foreground">{identity.role.replace('_',' ')}</p></div></div><Link href="/overview" className="tap-target flex items-center gap-2 rounded-xl px-3 text-sm font-medium text-muted-foreground hover:bg-secondary hover:text-foreground"><ArrowLeft className="h-4 w-4"/>Back to app</Link></div></header><div className="mx-auto grid max-w-[1500px] gap-6 px-4 py-6 lg:grid-cols-[220px_minmax(0,1fr)] lg:px-8"><aside className="min-w-0"><AdminNav/></aside><main className="min-w-0">{children}</main></div></div>}
