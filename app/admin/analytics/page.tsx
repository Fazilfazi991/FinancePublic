import { requireAdmin } from '@/lib/admin-auth';
import { getAnalyticsReport } from '@/lib/admin-data';
import { DateRangeFilter } from '@/components/admin/date-range-filter';
import { FeatureAdoption, MetricCard, TrendChart } from '@/components/admin/dashboard-ui';
export const dynamic='force-dynamic';
type Params={preset?:string;from?:string;to?:string};
export default async function ProductAnalytics({searchParams}:{searchParams:Promise<Params>}){const params=await searchParams,{db}=await requireAdmin(),{range,report}=await getAnalyticsReport(db,params),a=report.active;return <div className="space-y-6"><header className="flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between"><div><h1 className="text-3xl font-bold tracking-tight">Feature adoption</h1><p className="mt-1 text-sm text-muted-foreground">Distinct authenticated users and qualifying actions. Private financial values are never included.</p></div><DateRangeFilter range={range}/></header><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Period active users" value={a.selected} previous={a.previous_selected} help="Distinct authenticated users with qualifying activity during this period."/><MetricCard label="DAU" value={a.dau} help="Distinct qualifying users today in UTC."/><MetricCard label="WAU" value={a.wau} help="Distinct qualifying users over trailing 7 days."/><MetricCard label="MAU" value={a.mau} help="Distinct qualifying users over trailing 30 days."/></section><article className="rounded-2xl bg-card p-5 shadow-sm"><h2 className="text-lg font-semibold">Adoption by feature</h2><p className="mb-6 text-sm text-muted-foreground">Bars compare unique users; each row also shows total actions.</p><FeatureAdoption rows={report.featureAdoption} activeUsers={a.selected}/></article><article className="rounded-2xl bg-card p-5 shadow-sm"><h2 className="text-lg font-semibold">Active-user trend</h2><p className="mb-6 text-sm text-muted-foreground">Distinct users by UTC calendar day.</p><TrendChart data={report.activeTrend} series={[{key:'users',label:'Active users',color:'#7c3aed'}]} label="Active-user trend"/></article></div>}
