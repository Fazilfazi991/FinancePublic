'use client';export default function ErrorPage({reset}:{error:Error;reset:()=>void}){return <div role="alert" className="rounded-2xl bg-card p-8 shadow-sm"><h1 className="text-xl font-semibold">Admin data could not be loaded</h1><p className="mt-2 text-sm text-muted-foreground">Check the database migration and try again. No sensitive error details were exposed.</p><button onClick={reset} className="mt-5 h-11 rounded-xl bg-primary px-4 font-semibold text-primary-foreground">Try again</button></div>}
