export function RegistrationChart({data}:{data:{date:string;value:number}[]}){const max=Math.max(1,...data.map(x=>x.value));if(!data.some(x=>x.value))return <div className="grid min-h-52 place-items-center rounded-xl bg-secondary/50 p-6 text-center text-sm text-muted-foreground">No registrations in this period yet.</div>;return <div className="flex h-56 items-end gap-1 overflow-hidden" role="img" aria-label="Registrations over time">{data.map(x=><div key={x.date} className="group relative flex min-w-0 flex-1 flex-col justify-end"><div className="rounded-t-sm bg-primary/80 transition-colors group-hover:bg-primary" style={{height:`${Math.max(4,x.value/max*100)}%`}}/><span className="sr-only">{x.date}: {x.value}</span></div>)}</div>}
