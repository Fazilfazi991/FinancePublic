'use client';
import Link from 'next/link';import { usePathname } from 'next/navigation';import { BarChart3,Users,ChartNoAxesCombined,MousePointerClick,PlugZap,ScrollText } from 'lucide-react';import { cn } from '@/lib/utils';
const items=[['Overview','/admin',BarChart3],['Users','/admin/users',Users],['Product Analytics','/admin/analytics',ChartNoAxesCombined],['Acquisition','/admin/acquisition',MousePointerClick],['Integrations','/admin/integrations',PlugZap],['Audit Log','/admin/audit-log',ScrollText]] as const;
export function AdminNav(){const path=usePathname();return <nav aria-label="Admin" className="flex gap-1 overflow-x-auto pb-2 lg:flex-col lg:overflow-visible">{items.map(([label,href,Icon])=><Link key={href} href={href} className={cn('tap-target flex shrink-0 items-center gap-2 rounded-xl px-3 py-2 text-sm font-medium',path===href||href!=='/admin'&&path.startsWith(href)?'bg-primary text-primary-foreground':'text-muted-foreground hover:bg-secondary hover:text-foreground')}><Icon className="h-4 w-4"/><span>{label}</span></Link>)}</nav>}
